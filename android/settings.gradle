pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        maven { url "https://jitpack.io" } // agar tu jitpack libs use karta hai
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS) // ye check karega ke repositories sahi jagah likhi hain
    repositories {
        google()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}

